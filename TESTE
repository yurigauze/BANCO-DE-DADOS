

DELIMITER // 
CREATE TRIGGER venda_inativo
AFTER INSERT ON venda
FOR EACH ROW 
BEGIN
	DECLARE ATIVO CHAR(1);
    SELECT ATIVO INTO ATIVO FROM CLIENTE;
	IF ATIVO = "I" THEN
		SIGNAL sqlstate '45000' SET message_text "CLINTE INATIVO";
    END IF;
END;
//
DELIMITER ;

Select @meu_var;
Select
